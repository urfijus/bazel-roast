filegroup(
    name = "util",
    srcs = ["util.txt"],
)

genrule(
    name = "app",
    srcs = [
        "main.txt",
        ":util",
    ],
    outs = ["app.out"],
    cmd = "cat $(location main.txt) $(location util.txt) > $@",
)
