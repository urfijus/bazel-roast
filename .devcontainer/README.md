This devcontainer installs bazelisk and symlinks it to `bazel`.

Note: this requires network access (Codespaces has it). If you want a fully offline workshop, you can pre-bake bazel/bazelisk into a custom image.
